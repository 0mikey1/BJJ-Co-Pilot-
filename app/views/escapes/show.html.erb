<p style="color: green"><%= notice %></p>

<%= render @escape %>

<div>
 

  <div class ='text-center'> 

  <%= button_to "Add to Bag", add_escape_to_bag_path(@escape), method: :post, class: "btn btn-primary" %>
  <%= button_to "Add to Bucket", add_escape_to_bucket_path(@escape), method: :post, class: "btn btn-primary" %>
  <%= link_to "Back to escapes", escapes_path, class: "btn btn-secondary"%>
</div>

</div>
