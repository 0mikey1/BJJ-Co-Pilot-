<p style="color: green"><%= notice %></p>

<h1>Escapes</h1>

<div id="escapes">
  <% @escapes.each do |escape| %>
    <%= render escape %>
    
  <% end %>
</div>

<%= link_to "New escape", new_escape_path %>
